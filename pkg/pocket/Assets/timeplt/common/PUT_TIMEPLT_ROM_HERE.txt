Put timeplt.rom in this folder.

Build it from your own MAME timeplt romset with the builder shipped
alongside the core:

    python3 mra_build.py timeplt.mra timeplt.zip

It checks every ROM's CRC32 and verifies the finished 53,824-byte image,
so a wrong or bad romset is reported rather than quietly built.
